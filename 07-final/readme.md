# 7. Final

The finished app. The only addition over step 6 is `darkModeToggle: true` on every page's `properties` — the dark-mode toggle in the page header cycles light → dark → system and persists to `localStorage`.
